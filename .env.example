# Q-Shield sample environment file
# Copy this file to .env locally and fill values outside Git.

APP_ENV=development
LOG_LEVEL=INFO

# Example only. Do not commit real keys, tokens, or passwords.
API_BASE_URL=http://localhost:8000
API_TOKEN=replace-with-local-dev-token

# Database example
DATABASE_URL=sqlite:///qshield_local.db
